import React from 'react';
import PropTypes from 'prop-types';

const Preview = props => {

    let { value } = props

    return (
        <div>
            
        </div>
    )
}

Preview.propTypes = {
    value: PropTypes.string,
}

export default Preview